# QUERIES

type Query {
  oneUser(input: GetUser): User!
  oneRoadmap(input: GetRoadmap): Roadmap!
  allUsers: [User!]!
  allRoadmaps: [Roadmap!]!
  me: User @isAuthenticated
}

# INPUTS

input GetUser {
  userId: Int!
}

input GetRoadmap {
  roadmapId: Int!
}
