# QUERIES

type Query {
  user(input: GetUser): User!
  roadmap(input: GetRoadmap): Roadmap!
  users: [User!]!
  roadmaps: [Roadmap!]!
  me: User @isAuthenticated
}

# INPUTS

input GetUser {
  id: Int!
}

input GetRoadmap {
  id: Int!
}
